import { describe, expect, test } from "bun:test";

// mergeEnsembleResults was removed — inline ensemble merging is now in ensembleLlmCall.
// These tests tested the removed standalone function and are no longer needed.
